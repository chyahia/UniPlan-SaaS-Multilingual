# المساهمة في مشروع UniPlan SaaS 🤝

شكراً لاهتمامك بالمساهمة في تطوير UniPlan! نحن نرحب بمساهمات الأكاديميين والمطورين لتحسين خوارزميات الجدولة، إصلاح الأخطاء، أو إضافة ميزات جديدة تخدم المجتمع الجامعي.

يرجى قراءة هذه الإرشادات قبل البدء:

## ⚖️ التزام قانوني (هام جداً)
بمجرد تقديمك لأي مساهمة (Pull Request) في هذا المستودع، فإنك توافق صراحةً على أن كودك سيُدمج تحت رخصة المشروع الحالية **(CC BY-NC 4.0)**، والتي تمنع الاستخدام التجاري أو الربحي للنظام.

## 🐛 الإبلاغ عن الأخطاء (Issues)
إذا واجهت مشكلة أثناء تشغيل النظام أو الخوارزميات، يرجى فتح `Issue` وتضمين:
1. وصف دقيق للمشكلة.
2. خطوات إعادة إنتاج الخطأ (Steps to reproduce).
3. صورة للشاشة (إن أمكن) أو رسالة الخطأ من الشاشة السوداء (Celery/Flask logs).

## 💡 اقتراح ميزات جديدة
إذا كانت لديك فكرة لتحسين خوارزميات التوزيع (مثل إضافة قيد بيداغوجي جديد)، نرجو منك فتح `Issue` للنقاش حول الفكرة أولاً قبل البدء بكتابة الكود، للتأكد من توافقها مع المعمارية الحالية للنظام.

## 🛠️ خطوات تقديم الكود (Pull Requests)
1. قم بعمل **Fork** للمستودع إلى حسابك الشخصي.
2. قم بإنشاء فرع جديد لميزتك: `git checkout -b feature-name`
3. تأكد من أن الكود الخاص بك لا يكسر خاصية العزل (`tenant_id`) ولا يعطل عمل `Celery`.
4. التزم بكتابة تعليقات توضيحية للأكواد المعقدة (خاصة في ملفات الخوارزميات).
5. قم بعمل **Commit** لتغييراتك: `git commit -m "Add some feature"`
6. ارفع التغييرات إلى فرعك: `git push origin feature-name`
7. افتح **Pull Request** واشرح ما قمت بتعديله بالتفصيل.

## 👨‍💻 إعداد بيئة التطوير
النظام يستخدم Flask و Celery. تأكد من إعداد البيئة الوهمية وتثبيت المتطلبات من `requirements.txt`، وتشغيل Celery في نافذة طرفية مستقلة لضمان عمل خوارزميات الجدولة بشكل سليم.

شكراً لوقتك وجهدك في دعم هذا المشروع الأكاديمي!