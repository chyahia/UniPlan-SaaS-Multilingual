# 🏛️ UniPlan SaaS Ecosystem
[![License: CC BY-NC 4.0](https://licensebuttons.net/l/by-nc/4.0/88x31.png)](https://creativecommons.org/licenses/by-nc/4.0/)

**موزع الجداول الدراسية وحراسة الامتحانات الذكي (الإصدار السحابي المتعدد الأقسام)**

نظام سحابي (SaaS) متكامل ومبني لخدمة المؤسسات الأكاديمية والجامعات. يوفر المنصة بيئة معزولة لكل قسم أو كلية (Tenant Architecture)، مع لوحة تحكم مركزية للإدارة العليا. يعتمد النظام على خوارزميات ذكية (Metaheuristics) لتوليد الجداول الدراسية وتوزيع حراسات الامتحانات بشكل آلي وعادل.

---

## ✨ ميزات النظام الرئيسية

### 1. لوحة الإدارة المركزية (Super Admin)
* إدارة الأقسام والكليات (Tenants) ببيئات بيانات معزولة تماماً.
* التحكم في صلاحيات الوصول للأنظمة (تفعيل/تعطيل نظام التدريس أو الامتحانات لكل قسم بضغطة زر).
* إحصائيات شاملة ومراقبة للنظام.

### 2. 🗓️ نظام الجداول الدراسية (Teaching Module)
* إدخال وإدارة الأساتذة، المستويات، القاعات، والمواد.
* التمييز بين أنواع الدروس (محاضرات، أعمال موجهة، أعمال تطبيقية).
* خوارزميات جدولة ذكية تعمل في الخلفية (Background Tasks).
* واجهة "بوابة الأستاذ" لإدخال الرغبات والأيام المفضلة.

### 3. 🎓 نظام حراسة الامتحانات (Exams Module)
* تزامن سحري (Sync) لسحب الأساتذة والمواد والإسنادات مباشرة من نظام التدريس.
* إدارة فترات الامتحانات (أساسية/احتياطية) وبناء هيكل الأيام.
* ضبط قيود دقيقة: الإعفاءات، التنافر بين الأساتذة، الحد الأقصى للحراسة، ومراعاة فترات التدريس.
* التوزيع العادل للحراسات وتوليد الجداول وتصديرها.

---

## 🛠️ التقنيات المستخدمة (Tech Stack)

* **Backend:** Python, Flask, SQLAlchemy (ORM)
* **Background Processing:** Celery
* **Frontend:** HTML5, CSS3, JavaScript (Vanilla)
* **Architecture:** Multi-tenant SaaS (Schema/Tenant-ID isolation)

---

## 🚀 طرق التشغيل (Deployment & Execution)

يتميز النظام بذكاء معماري (Hybrid Architecture) يجعله يتكيف تلقائياً مع بيئة التشغيل، حيث يمتلك وضعين مختلفين:

### 1. وضع السحابة / التطوير المحلي (Cloud / Local Development)
في هذا الوضع، يعتمد النظام على `Celery` و `Redis` لمعالجة خوارزميات التوزيع الثقيلة في الخلفية لضمان عدم تجمد خادم الويب.

* **تثبيت المتطلبات:**
  ```bash
  pip install -r requirements.txt

* 1.تشغيل خادم العمليات الخلفية (Celery): (يُفضل استخدام --pool=solo لتوافق أفضل مع أنظمة Windows)
  ```bash
  celery -A celery_worker.celery_app worker --loglevel=info --pool=solo

* 2.تشغيل تطبيق Flask:
  ```bash
  python run.py

### 2.  وضع سطح المكتب المستقل (Desktop Executable Mode)
عند تجميع النظام وتحويله إلى تطبيق سطح مكتب (ملف .exe) للمستخدم النهائي، يقوم النظام بذكاء باكتشاف البيئة وإجراء التعديلات التلقائية التالية:

الاستغناء عن Celery: يتم تعطيل خادم المهام السحابي، ويعتمد النظام بدلاً من ذلك على خيوط المعالجة المحلية (Threading) والذاكرة الحية (RAM) لتسجيل الأحداث الحية.

حماية البيانات: يتم التوجيه التلقائي لمسار حفظ قاعدة البيانات (.db) إلى المجلد الآمن للنظام (AppData/Roaming) لتجنب مشاكل الصلاحيات (Permission Errors) وضمان عدم ضياع بيانات الأقسام عند تحديث البرنامج.


## ⚖️ الترخيص وحقوق الاستخدام (License & Copyright)
تم تصميم وتطوير هذا النظام لخدمة المجتمع الأكاديمي والجامعي. هذا المشروع متاح بموجب رخصة الإبداع المشاع نَسب المُصنَّف - غير تجاري 4.0 دولي (CC BY-NC 4.0).

يُسمح لك بنسخ الكود، دراسته، وتعديله للاستخدامات الأكاديمية والمجتمعية، ولكن يُمنع منعاً باتاً استخدامه، أو استضافته، أو بيعه كخدمة لأي غرض تجاري أو ربحي بأي شكل من الأشكال دون الحصول على إذن كتابي مسبق من المؤلف الأصلي.

تم تطوير هذه المنصة لتحديث ورقمنة العمليات الأكاديمية الجامعية وتقليل الجهد الإداري.

## 👨‍💻 المطور: د. شعيب يحيى